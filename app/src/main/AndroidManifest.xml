<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.captivators.touristapp" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Dashboard" >
        </activity>
        <activity
            android:name=".SplashActivity"
            android:configChanges="uiMode"
            android:theme="@style/AppTheme.Launcher" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ImageScan" android:label="Get info" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".ListOfPlaces" android:label="Plan a Trip" android:theme="@style/Theme.AppCompat.DayNight"/>
        <activity android:name=".IndiaGateInfo" android:label="India Gate" android:theme="@style/Theme.AppCompat.DayNight"/>
        <activity android:name=".TajMahalInfo" android:label="Taj Mahal" android:theme="@style/Theme.AppCompat.DayNight"/>
        <activity android:name=".LotusTempleInfo" android:label="Lotus Temple" android:theme="@style/Theme.AppCompat.DayNight"/>
        <activity android:name=".HavaMahalInfo" android:label="Hava Mahal" android:theme="@style/Theme.AppCompat.DayNight"/>
        <activity android:name=".TajMahalMaps" />
        <activity android:name=".LotusTempleMaps" />
        <activity android:name=".HavaMahalMaps" />
        <activity android:name=".IndiaGateMaps" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>